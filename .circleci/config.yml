version: 2.1

workflows:
  build-workflow:
    jobs:
      - runner
jobs:
  runner:
    machine: true
    resource_class: fy9havcxxh2v26vozdiaju/res
    steps:
      - run: 
        name: Greetings
        command: echo "Hi I'm on Runners! sajjdsk"

      - run:
        name: Install Deps
        command: port install pkg-config freetype jpeg-turbo libheif libpng libtiff libtool openexr openexr openjpeg webp libraw ghostscript

        
